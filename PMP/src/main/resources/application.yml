spring:
  application:
    name: spring-data-demo
  datasource:
    password: Incorrect@7
    url: jdbc:postgresql://localhost:9090/pmpDB
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
server:
  port: 8080